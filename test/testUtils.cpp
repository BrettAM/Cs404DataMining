#include "UnitTest++/UnitTest++.h"
#include "util.hpp"

/*TEST(Step) {
    CHECK_EQUAL(step(-1.0), 0.0);
    CHECK_EQUAL(step(-0.5), 0.0);
    CHECK_EQUAL(step( 0.0), 1.0);
    CHECK_EQUAL(step( 0.5), 1.0);
    CHECK_EQUAL(step( 1.0), 1.0);
}

TEST(Sigmoid) {
    double prev = 0.0;
    for(double d = -10; d<10; d+=0.5){
        double v = sigmoid(d);
        CHECK(v >= prev); //increasing
        CHECK(v >= -1); //minimum of -1
        CHECK(v <=  1); //maximum of  1
    }
}

*/
